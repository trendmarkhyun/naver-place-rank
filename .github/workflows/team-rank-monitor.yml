# Supabase Rank Monitor(supabase-rank-monitor.yml)로 대체됨.
# 필요 시 수동 실행만 허용 (JSON commit 방식 비활성).

name: Team Rank Monitor (legacy)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Use Supabase workflow instead
        run: |
          echo "이 워크플로는 비활성화되었습니다."
          echo "Actions → Supabase Rank Monitor 를 사용하세요."
